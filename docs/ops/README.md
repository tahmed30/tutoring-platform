# Ops notes

`last-deploy-diagnosis.md` is written by the **Cursor AWS Diagnose** GitHub Action when a Deploy workflow fails (Cursor CLI + AWS OIDC). Prefer the Actions artifact / auto-opened issue over committing that file.
